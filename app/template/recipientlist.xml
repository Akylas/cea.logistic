<ListView items="{{ filteredRecipientItems }}"  itemTap="{{recipientItemTap}}" rowHeight="50">
    <ListView.itemTemplate>
            <Label text="{{ name }}" verticalAlignment="center" textWrap="true" color="black" fontSize="16" padding="5" />
    </ListView.itemTemplate>
</ListView>